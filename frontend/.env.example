# Copy this file to .env and adjust if your backend runs somewhere other than
# localhost:5000. Vite only exposes variables prefixed VITE_ to client code.

VITE_API_BASE_URL=http://localhost:5000/api/v1
